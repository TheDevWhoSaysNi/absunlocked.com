# absunlocked.com

Static “domain for sale” site for [absunlocked.com](https://absunlocked.com).

Hosted on Cloudflare Workers static assets (same edge hosting model as Pages). Email stays on Cloudflare Email Routing.

```bash
npx wrangler deploy
```
